원래는 코사인 유사도를 비교하려고 했으나, 제가 설명을 들어도 어떻게 적용해야할  지 몰라서
realtive location을  비교하는 걸로 작성 했습니다.
오차가 어느정도 있긴 하지만 보완하는 코드를 작성했습니다.
GmcompareValues 이  함수가 블루프린트에서도  우클릭하면  쓸 수  있는데 소수값 하나를  받습니다.
그 안에서 비긴  플레이 바로 다음에 연결해둔 함수  GmGetAllSocketNames()이 양쪽의 모든 본들을 가져옵니다
IsNearlyEquals()함수로 허용될  오차값에  대한 범위는 소수값 파라미터로 받아옵니다.
GetsocketTransform이 함수가 절대월드값으로 가져오는데, 상대적인 위치가 필요해서 각 캐릭터의 루트본에 대한 절대 월드 포지션으로
각 상대적 거리값이랑 회전값까지 변환을  해줍니다.

결론적으로는 bResult가 bool 형식으로 true와 false를 리턴합니다.
오차가 크지 않으면 true 오차가 크다면 false를 리턴합니다.
그래서 perfect good bad 보다는 good과 bad 두 가지로 나타내면  좋을 것 같습니다.
